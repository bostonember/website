<h4>Add New Talk</h4>

<form class="form" {{action 'savePresentation' on='submit'}}>
  <div class="row">
    {{presentation-form
      presentation=model.presentation
      useMeetupSelector=true
      meetups=model.meetups
      speakers=model.speakers}}
  </div>

  <div class="form-group">
    <input type="submit" class="btn btn-primary" value='Create' disabled={{isSubmitting}}>
  </div>
</form>
